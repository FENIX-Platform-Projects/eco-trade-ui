define({
    region: {
        container: '#page_region',
        filters: {
            year: '#filter_year',
            trade_flow: '#filter_trade_flow_code',
            commodity: '#filter_commodity_code'
        }
    },
    country:{
        container: '#page_countries',
        filters: {
            partner_code: '#filter_partner_code_country',
            year: '#filter_year_country',
            trade_flow: '#filter_trade_flow_code',
            commodity: '#filter_commodity_code_country'
        }
    }
});
